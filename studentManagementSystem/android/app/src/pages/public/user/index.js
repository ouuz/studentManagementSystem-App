import React, { useState } from 'react';
import {
  StyleSheet,
  View,
  TextInput,
  TouchableOpacity,
  ImageBackground,
  Alert,
  Image
} from "react-native";

const User = () => {
  return (
    <View></View>  
  );
};

const style = StyleSheet.create({
});

export default User;